import type { RegisteredRouter } from '@tanstack/react-router';

/**
 * Type representing the registered router for type-safe navigation.
 * Used with LinkOptions for proper type inference.
 */
export type RouteIds = RegisteredRouter;
